version: '3.8'

services:
  macedoazevedoapp:
    container_name: macedoazevedoapp
    image: macedoazevedoapp
    build: .
    ports:
      - "80:8000"
    stdin_open: true 
    tty: true
    env_file: .env
    volumes:
      - ./code:/home/app
